> *AI generated content*

# 01 - Bootstrap

Creates the S3 bucket used to store Terraform state files for all other modules.

## Usage
```bash
terraform init
terraform plan
terraform apply
```

## Variables
| Name | Description |
|------|-------------|
| `AWS_REGION` | AWS region |
| `state_bucket_name` | Name of the S3 state bucket |
